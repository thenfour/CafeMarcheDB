# Release tooling defaults (non-secret). Commit this file.
# Override values in .env.release.local (git-ignored).

# Fine-grained GitHub token is recommended to be set in .env.release.local
# GITHUB_TOKEN=

# Rolling tag to move when publishing (the "latest published" pointer)
ROLLING_TAG=latest
