{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch release",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/index.js",
            "preLaunchTask": "build:release" 
        },
        {
            "name": "Launch debug",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/index.js",
            "preLaunchTask": "build:debug" 
        }
    ]
}
