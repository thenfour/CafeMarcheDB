# Deploy/Upgrade defaults (non-secret). Commit this file.
# Override values in .env.deploy.local (git-ignored).

# GitHub token is read from .env.deploy.local; do not set secrets here.
# GITHUB_TOKEN=

# Supervisor service name
SERVICE=cmdb

# Database name for backups
DB_NAME=tenfour_cmdb

# Where to write DB backups on the server (will be created if missing)
BACKUP_DIR=~/backups

# How many releases to display for selection
RELEASES_TO_SHOW=10
